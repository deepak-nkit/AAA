#include<stdio.h>
#include<conio.h>
void main()
{
FILE*p;
p=fopen("AAA.txt","w");
if (p==NULL)
   {printf("file not found");
   }
else
   {printf("file founded");
   
   while (i<=3000)
   {fprintf(p,"\n hello world");
   i++;
   }
    }
   fclose(p);
getch();
}
